// In development, Vite proxies /api → localhost:8000
// In production, set VITE_API_BASE to your deployed backend URL (e.g. https://chemvision-api.onrender.com)
// If not set, defaults to '' which means same-origin (for Vite proxy in dev)
VITE_API_BASE=
